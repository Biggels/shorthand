[ ] Add a separate module that accepts a string of words and returns the whole string with any abbreviatable words subbed out
[ ] Is it possible make a discord bot that replaces your messages? probably not, but you could make one that responds with the abbrev'd version of your message
